#include <stdio.h>
/*
 * main - entry point
 *
 * Return:always return 0
 *
 */
 int main(void)
 {
 int i;

 for (i = 10; i < 20; i++)
 {
 putchar((i % 10) + '0');
 if (i != 19;#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int d;

	for (d = '0'; d <= '9'; d++)
	{
		putchar(d);
		if (d != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');

	return (0);
}
 {
 putchar(',');
 putchar(' ');
 }
 }
 putchar('\n');
 return i(0);
 }
